<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Karunadavaanya">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Karunadavaanya">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <activity
            android:name=".WildlifeWikiActivity"
            android:exported="false"
            android:label="@string/wildlife_wiki" />

        <activity
            android:name=".ForestSoundsActivity"
            android:exported="false"
            android:label="Forest Sounds" />

        <activity
            android:name=".MovementAlertActivity"
            android:exported="false"
            android:label="@string/movement_alerts" />

        <activity
            android:name=".ReportAlertActivity"
            android:exported="false"
            android:label="Report Movement" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:label="About App" />

        <activity
            android:name=".AnimalDetailActivity"
            android:exported="false"
            android:label="@string/animal_detail" />
    </application>

</manifest>
